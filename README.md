# Internship_Flask_TASK_4_URL-Shortener-Web-Application-Basic-
A Flask-based URL Shortener web application that validates URLs, generates unique short links, stores history using SQLite and SQLAlchemy ORM, supports redirection, and provides a clean Bootstrap-based interface with copy-to-clipboard functionality.
